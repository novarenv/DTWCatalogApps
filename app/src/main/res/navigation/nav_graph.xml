<?xml version="1.0" encoding="utf-8"?>
<navigation xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/nav_graph"
    app:startDestination="@id/mainActivity">

    <activity
        android:id="@+id/mainActivity"
        android:name="com.example.trainhelloworld.MainActivity"
        android:label="activity_main"
        app:action="onClick"
        tools:layout="@layout/activity_main" />
    <activity
        android:id="@+id/detailListView"
        android:name="com.example.trainhelloworld.DetailListView"
        android:label="activity_detail_list_view"
        tools:layout="@layout/activity_detail_list_view" />
</navigation>